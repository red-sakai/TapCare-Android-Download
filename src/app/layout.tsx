import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TapCare - Emergency Response App",
  description:
    "Quick access to emergency services, school safety resources, and immediate help when you need it most. For Rizal High School Students.",
  icons: {
    icon: "/assets/tapcare-logo.png",
    shortcut: "/assets/tapcare-logo.png",
    apple: "/assets/tapcare-logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
